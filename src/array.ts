const allTypes = ["shakeeb", 1, true];
const students: string[] = ["shakeeb", "rakib", "sumon"];
const roleNumber: number[] = [1, 2, 3, 4, 5, 5];

//tuple type array is a special type of array where we can define the type of array
// example :
const student: [string, number] = ["shakeeb", 1];
